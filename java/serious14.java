//tree map to sorted map-> in between

// a map is an object that stores the association between keys and values / key value pair. 
// given a key , we can find its value
// keys nd values both are objects. keys should be unique but vals can be duplicate.
// this map dont implement iterable interface, we cant do cycle through the map using iterator and for-each loop. 
// but we can obtain a collection view of map , which allows the use of either for loop or iterator

//  map interface: it maps the unique keys to vals
//  map.Entry : decribes the element (key/val back). here Entry is inner class of map.
//  navigable map : it implements sorted map to handel retrival of entries based on closest match
//  SortedMap : extends map interface so that keys can be maintained in ascending order.
//  set interface: Interface Set<E>
//  set interface: Interface Map<K,V> 

// public class serious14 {
    
// }
